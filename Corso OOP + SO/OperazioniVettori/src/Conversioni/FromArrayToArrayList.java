package Conversioni;

public class FromArrayToArrayList
{
	/*
	 * 1) Dichiaro l'Array -> Element[] array = { new Element(1), new Element(2), new Element(3) };
	 * 2) Converto in ArrayList -> List<Element> elementi = new ArrayList(Arrays.asList(array));
	 */
}
